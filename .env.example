# ─────────────────────────────────────────────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored. This file (.env.example) is safe to commit.
# ─────────────────────────────────────────────────────────────────────────────

# ── Sanity: Public (safe for browser) ────────────────────────────────────────

# Your Sanity project ID — found at sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=

# Dataset name — typically "production"
NEXT_PUBLIC_SANITY_DATASET=production

# API version — pin to a specific date, never use "latest"
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# ── Sanity: Server-only (NEVER prefix these with NEXT_PUBLIC_) ───────────────

# Read token — Viewer role, generated at sanity.io/manage > API > Tokens
# Required for server-side queries. Never expose to the browser.
SANITY_API_READ_TOKEN=

# Webhook secret — used to validate on-demand ISR revalidation requests
# Generate with: openssl rand -base64 32
SANITY_REVALIDATE_SECRET=
