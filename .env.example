# ─── Payload CMS ─────────────────────────────────────────────────────────────
# Secret for signing Payload JWTs — must be at least 32 chars
# Generate one: openssl rand -hex 32
PAYLOAD_SECRET=your_payload_secret_min_32_chars_here

# ─── Database ─────────────────────────────────────────────────────────────────
# Dev (Docker):  postgresql://payload:password@localhost:5432/payload
# Prod (Neon):   postgres://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://payload:password@localhost:5432/payload

# ─── Vercel Blob (production media storage) ───────────────────────────────────
# Create a Blob store in your Vercel project → Storage → Blob
# Not needed in dev — files are stored locally in /public/media
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxx

# ─── Contact form (Resend) ────────────────────────────────────────────────────
# Sign up at https://resend.com → API Keys → Create key
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# The inbox that receives contact form submissions
CONTACT_TO_EMAIL=you@yourdomain.com
