# ─── Sanity CMS ──────────────────────────────────────────────────────────────
# Found in your Sanity project settings → API
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# A read token with Viewer permissions — create one in Sanity → API → Tokens
SANITY_API_READ_TOKEN=your_sanity_read_token

# A random secret (min 32 chars) shared with the Sanity webhook for cache revalidation
# Generate one: openssl rand -hex 32
SANITY_REVALIDATE_SECRET=your_revalidate_secret_min_32_chars

# ─── Contact form (Resend) ───────────────────────────────────────────────────
# Sign up at https://resend.com → API Keys → Create key
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# The inbox that receives contact form submissions
CONTACT_TO_EMAIL=you@yourdomain.com
